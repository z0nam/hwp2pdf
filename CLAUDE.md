# Claude Code context

Read and follow `AGENTS.md`, then read `docs/context.md` and `docs/ai-context.md` before making changes.

Use `C:\Users\user\dev\hwp2pdf` as the canonical path. The `_projects` path is a temporary compatibility junction and must not become a new dependency.

Historical Claude and other-agent transcripts are preserved locally under `.ai-context/archive/` and excluded from Git. Use them only when the summarized context is insufficient.
